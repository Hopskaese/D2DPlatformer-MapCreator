#include "Dummy.h"


/*
void Dummy::Draw(Graphics* pGraphics)
{
	if (!m_pSpriteSheet) return;

	pGraphics->GetRenderTarget()->DrawBitmap(
		m_pSpriteSheet->GetBitmap(),
		D2D1::RectF(m_fX, m_fY, m_fX + m_pSpriteSheet->GetBitmap()->GetSize().width, m_fY + m_pSpriteSheet->GetBitmap()->GetSize().height),
		1.0f,
		D2D1_BITMAP_INTERPOLATION_MODE_NEAREST_NEIGHBOR,
		D2D1::RectF(0, 0, m_pSpriteSheet->GetBitmap()->GetSize().width, m_pSpriteSheet->GetBitmap()->GetSize().height)
	);
}
*/